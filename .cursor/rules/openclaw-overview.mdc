---
description: OpenClaw 项目架构与本地前后端启动命令（每次会话都注入）
alwaysApply: true
---

# OpenClaw · 项目总览与本地启动

> 仓库根仍以 `AGENTS.md` 为权威协作规范；本规则仅保留架构速读与前后端启动指令。

## 项目架构（精简）

- Monorepo（`pnpm workspace`），TypeScript ESM，Node 22+。
- 后端核心：`src/`（CLI、runtime、gateway）。
  - `src/gateway/`：HTTP/WS 网关、鉴权、会话与工具调度。
  - `src/gateway/protocol/`：协议定义（客户端/服务端共享）。
  - `src/plugins/` + `src/plugin-sdk/`：插件加载与 SDK 门面。
- 前端：`ui/`（Vite + TS），开发时走 `ui:dev`，产物可由 gateway 托管。
- 扩展：`extensions/<id>/`，通过 `openclaw/plugin-sdk/*` 与核心交互。

## 启动环境与命令（精简）

运行环境：

- Node `>=22.14.0`
- `pnpm`
- 配置文件：`~/.openclaw/openclaw.json`

本机若系统 Node 版本偏低，先注入 Node 22：

```bash
export PATH="/home/ubuntu/.local/node22/node-v22.16.0-linux-x64/bin:$PATH"
node --version
```

首次准备：

```bash
pnpm install
pnpm openclaw setup
pnpm ui:build
```

开发启动（两个终端）：

```bash
# 终端 A：后端 gateway（默认 127.0.0.1:18789）
export PATH="/home/ubuntu/.local/node22/node-v22.16.0-linux-x64/bin:$PATH"
pnpm gateway:watch

# 终端 B：前端 dev server（默认 localhost:5173）
export PATH="/home/ubuntu/.local/node22/node-v22.16.0-linux-x64/bin:$PATH"
pnpm ui:dev
```

前台直接启动网关（非 watch）：

```bash
export PATH="/home/ubuntu/.local/node22/node-v22.16.0-linux-x64/bin:$PATH"
pnpm openclaw gateway
```
